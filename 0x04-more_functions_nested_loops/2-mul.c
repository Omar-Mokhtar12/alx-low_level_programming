#include "main.h"
/**
 *mul - function that multiplies two integers.
 *@a: first number
 *@b: second number
 *Return: Always return sum of multiblication
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
