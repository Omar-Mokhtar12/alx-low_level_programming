#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string.
 *
 *@s: the string that will get the lenght.
 *
 * Return: return length of the string.
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
