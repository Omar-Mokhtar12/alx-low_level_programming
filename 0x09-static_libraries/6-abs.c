#include "main.h"
#include <stdlib.h>
/**
 *_abs - function that computes the absolute value of an integer.
 *@x: The number we want to get the abs from it
 *Return: Always return a positive number
 */
int _abs(int x)
{
	return (abs(x));
}
