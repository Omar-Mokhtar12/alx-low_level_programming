#include "main.h"

/**
 *main - prints 10 times the alphabet, in lowercase, followed by a new line.
*/
void print_alphabet_x10(void)
{
	int n = 1;
	char ch;
	
	while (n <= 10)
	{
		for (ch 
		n++;
	}
}
